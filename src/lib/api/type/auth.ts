export type AuthDTO = {
    username: string,
    password: string,
}

export type AuthDTOResp = {
    username: string,
    user_id: string,
}

export type User = {
    username: string, 
    userID: string,
}