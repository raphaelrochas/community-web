export type AuthDTO = {
    username: string,
    password: string,
}

export type AuthDTOResp = {
    username: string,
    id: string,
}

export type User = {
    username: string, 
    userID: string,
}